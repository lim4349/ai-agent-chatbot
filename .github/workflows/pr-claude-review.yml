name: PR - Claude Code Review

on:
  pull_request:
    branches: [dev, main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  claude-review:
    name: Code Review (GLM-5)
    runs-on: ubuntu-latest
    if: |
      github.actor != 'dependabot[bot]' &&
      !contains(github.event.pull_request.labels.*.name, 'skip-review')

    steps:
      - name: Checkout PR
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Debug Secret
        run: |
          if [ -z "${{ secrets.GLM_API_KEY }}" ]; then
            echo "❌ GLM_API_KEY is NOT set"
            exit 1
          else
            echo "✅ GLM_API_KEY is set (length: ${#GLM_KEY})"
          fi
        env:
          GLM_KEY: ${{ secrets.GLM_API_KEY }}

      - name: Get changed files
        id: files
        run: |
          git fetch origin ${{ github.base_ref }} --depth=1
          files=$(git diff --name-only origin/${{ github.base_ref }}...HEAD | head -20 | tr '\n' ', ')
          echo "changed_files=$files" >> $GITHUB_OUTPUT
          echo "Files: $files"

      - name: Run GLM-5 Code Review
        id: review
        env:
          GLM_API_KEY: ${{ secrets.GLM_API_KEY }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
          FILES: ${{ steps.files.outputs.changed_files }}
        run: |
          echo "Calling GLM-5 API..."

          # Create JSON payload
          payload=$(cat <<JSONEOF
          {
            "model": "glm-5",
            "max_tokens": 2000,
            "messages": [
              {
                "role": "user",
                "content": "다음 PR을 코드 리뷰해주세요:\n\n제목: $PR_TITLE\n\n변경된 파일: $FILES\n\n다음 내용을 포함하여 한국어로 리뷰를 작성해주세요:\n1. 변경사항 요약\n2. 잠재적인 문제점\n3. AGENTS.md에 추가하면 좋을 프로젝트 규칙"
              }
            ]
          }
          JSONEOF
          )

          # Call API
          response=$(curl -s -X POST "https://api.z.ai/api/anthropic/v1/messages" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $GLM_API_KEY" \
            -d "$payload" 2>&1)

          echo "API Response:"
          echo "$response"

          # Extract content
          review_content=$(echo "$response" | jq -r '.content[0].text // "Error: No content received"')

          # Save to file
          echo "$review_content" > review_output.md

      - name: Post review comment
        if: success()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "## 🤖 GLM-5 Code Review" > comment.md
          echo "" >> comment.md
          cat review_output.md >> comment.md
          echo "" >> comment.md
          echo "---" >> comment.md
          echo "*This review was generated automatically by GLM-5*" >> comment.md
          gh pr comment ${{ github.event.pull_request.number }} --body-file comment.md || echo "Failed to post comment"
